CREATE TABLE favorites (
    id TEXT NOT NULL PRIMARY KEY
);

selectAll:
SELECT * FROM favorites;

isFavorite:
SELECT EXISTS(SELECT 1 FROM favorites WHERE id = ?);

insert:
INSERT OR REPLACE INTO favorites(id) VALUES (?);

delete:
DELETE FROM favorites WHERE id = ?;
